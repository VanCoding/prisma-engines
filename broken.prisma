datasource chinook {
  provider = "mysql"
  url      = "mysql://vt_app:@localhost:15306/commerce?connection_limit=1"
}

model TestModel {
  id          String  @id @default(cuid())
  optString   String?
  optInt      Int?
  optFloat    Float?
  optBoolean  Boolean?
  optDateTime DateTime?
}
